import React, { Component } from "react";
  import { Button } from "../Repeat";
export default class Btn extends Component {
    render() {
        return(
            <Button>Gift Finder</Button>
        )
    }
}